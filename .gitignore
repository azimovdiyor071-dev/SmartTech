# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local
# NOTE: dist/ is committed on purpose so Render can serve the prebuilt
# frontend without a build step. Rebuild with `npm run build` before pushing.

# Secrets & local data — never commit these
.env
.env.*
!.env.example
server/data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
